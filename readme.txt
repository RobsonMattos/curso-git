Curso git

 git --version
 git config

Ciclo básico
  .gitconfig 
  git config --global user.name “nome”
  git config --global user.email “nome@email”
 - init, ls-files, status, add(novos e altrados), commit -m "Texto", log

 colocar os arquivos no github
   git remote add origin url(http://github.com/conta...)
   git push origin master (Cria se não existit e atualiza alterações caso ja exista o arquivo no repositorio)

  obter repositorio
    git clone url

Ver bitbucket.org
rjnmattos@gmail.com/rjnmattos
google
#a1...#